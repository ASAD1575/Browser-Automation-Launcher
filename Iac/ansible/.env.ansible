# Environment variables for Ansible Windows login
# These are dynamically written in the GitHub Actions workflow

TF_VAR_WINDOWS_USERNAME=${WINDOWS_USERNAME}
TF_VAR_WINDOWS_PASSWORD=${WINDOWS_PASSWORD}
